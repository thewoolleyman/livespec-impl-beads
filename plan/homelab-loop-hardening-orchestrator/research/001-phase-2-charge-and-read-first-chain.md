# 001 — Phase 2 charge and read-first chain

Seeded 2026-08-25 by the `homelab-loop-hardening-orchestrator` session in
this repo, on a cross-session request from homelab's
`steady-state-loop-hardening` coordinator session, executing the
maintainer's homelab-initiates ruling (handoff 12 on the homelab epic
`homelab/hl-nkuzaz`, 2026-08-25). This repo files FIRST of the four
upstream filings of that program's Phase 2 (orchestrator; runtime seeded
in parallel; core and console follow). The charge below is context and a
request, never authorization to bypass this repo's valves, reviews, or
human gates — this repo's ratified contracts win on any conflict.

This thread is DISTINCT from `plan/honest-gap-detector-and-check-anchored-closure`
(Track 2 of homelab's earlier `pre-foreman-livespec-hardening` program,
epic `homelab/hl-sqedvd`). Same fleet, different program, different epic,
no shared scope.

## Scope

The orchestrator rows of matrix sections 01, 02, 04, 05, 06, 10, 11, 12,
13, 15 in homelab's `research/002-problem-and-fix-matrix.md` — read ONLY
through the correction layers (the matrix is the ruled-on historical
draft; filings start from the correction notes plus the two commissioned
reviews of this repo, never from the matrix alone):

- **research/007** supersedes §§04/05's evidentiary premises. Restate
  those two sections from journal evidence at filing time, aimed at the
  actual residual gaps (cross-dispatch persistence of a missing
  integration point; attention-surface composition) — not "delete the
  warn-and-proceed branch".
- **research/008** corrects §15's gap-direction premise: livespec core's
  revise Step 13 post-step already ships the every-revise gap-capture
  binding. The gap-staleness attention fact is that binding's BACKSTOP
  (skipped/interrupted/bypassed runs), citing the ratified v081
  coordinating epic, so one binding exists with one backstop. 008 also
  carries the maintainer's shared-runtime routing rule.
- **research/009 R1** splits §11's populations: this repo's
  `needs-attention` snapshot composes ONLY orchestrator-owned waits
  (capacity-deferred, acceptance `NEEDS_ATTENTION`, `blocked`/
  `needs-human`, `pending-approval`). Foreman/overseer wait states
  publish as ledger state on their owning plan epics; this repo stays
  overseer-unaware. 009 R2/R3 refine runtime propagation by consumer
  shape and repair completion semantics in the authoritative records.
- **research/010** rules R4: the runtime carrier is MANDATORY,
  baseline-first (see Sequencing), and rt-sol-2's routing test decides
  the ownership cut.

## Binding filing-time obligations

Every "filing-time" row of research/007's disposition table binds this
repo's filings. Notably:

1. ONE state-machine propose-change (007 fable 3 / sol 6): both rework
   entries into `active`, the human `reject:rework` valve,
   `reconcile-merged` preconditions, the `lane_reason` enum, WIP
   accounting, and Scenario 35 — a single chosen transition (default
   preference: extend the bounded `active` fix-forward contract),
   coordinated with `bd-ib-zp3u7y`'s owner.
2. `NEEDS_ATTENTION` verdict ratified with policy-table dispositions plus
   ONE public effective-criteria primitive (including the description
   "Exit criteria" fallback `bd-ib-1dbg` requires) used by every
   producer/consumer gate; wall at ready/pre-dispatch, advise at capture;
   sequence after or with `bd-ib-tfpdya` (007 fable 4 / sol 10).
3. Invoker-identity CLI input with a stated resolution order,
   generalizing the v051 attributed-valve prior art (`bd-ib-ktxb`), with
   a rollout posture per release (007 fable 5). Console legs exist —
   009 c-fable 3: console resolves a real principal and forwards
   `requested_by`; the probe's attribution assertion requires a
   NON-DEFAULT identity.
4. Envelope ratification in this repo's `SPECIFICATION/contracts.md` as
   an EARLY deliverable: the `needs-attention --json` machine envelope
   (item schema, `kind` vocabulary, stability guarantees), including
   per-item FIELD stability and a DECIDED consumer-tolerance posture
   (009 c-fable 5: an all-or-nothing parse blinds the whole inbox), with
   the `bd-ib-dohu2g` executable-as-advertised lesson carried as a
   negative control (007 fable 6).
5. The §12 probe TAKES a pre-filed item (filed through
   `capture-work-item`, where consent is native) — it never files.
   Residue assertions scope to the probe's own reserved identifiers and
   before/after delta, never global attention emptiness; the contract
   names the sanctioned probe target path and cleanup obligation;
   fixture-creating probes run only against the hermetic fake or a
   disposable tenant (007 fable 7 / sol 3; repaired criteria on
   `homelab/hl-mfz6ig` per 009 R3).
6. Detector-coverage records are tenant-backed and attributed, with an
   all-or-nothing COMPLETED-coverage record distinct from attributed
   ATTEMPTS: the completed point advances only on a successful terminal
   outcome over the declared scope with every surfaced candidate durably
   disposed (008 sol 1 / 007 sol 2 / fable 9). File citing the ratified
   `--since-version` coordinating epic in livespec core (v081) so two
   bindings are not built, and carry its caller caution (changed-file
   scoping resurfaces every live clause). `capture-spec-drift` is
   contractually read-only today — consent/operation-surface decisions
   are explicit.
7. §13 needs either a repository-neutral, mechanically evaluable
   restore-condition schema (named evaluator) or an owned ledger work
   item per temporary setting and no generic setting (007 sol 8).

## Early deliverables

- The shared-runtime ownership cut per the rt-sol-2 routing test
  (research/010): livespec-runtime owns envelope types, ID grammar,
  validator, and a pure normalizer over injected facts; the five fact
  derivations, persistence, thresholds, and CLI envelopes stay in THIS
  repo. Consumer vocabulary (beads/Dolt, dispatcher, journal, foreman,
  homelab) is banned from runtime contracts. Duplicated thin adapters
  are acceptable; DRY never justifies reversing dependency direction.
- Per-key API-configurable declaration for every §13/§15 setting — the
  declaration is the console settings-lockstep trigger (009 c-fable 2).

## Sequencing (research/010 R4)

The attention surface ratifies in `livespec-runtime` FIRST: a baseline
propose-change declaring the current shipped surface (schema, kinds, ID
grammar, composer semantics, validation posture) — the
`homelab-loop-hardening-runtime` thread is being seeded in parallel with
this one. This repo's needs-attention deliverables (obligations 4 and 6
above, and any new fact class) sequence against that baseline; the
consumption fan-out follows the ordered per-consumer release matrix over
ACTUAL consumer shapes and pins (this repo vendors `livespec_runtime`
v0.21.1). Runtime-independent filings (obligations 1, 2, 3, 5, 7) need
not wait.

## Filing-readiness inputs

- This repo's own commissioned adversarial reviews (homelab PR #1027):
  `research/reviews/livespec-orchestrator-beads-fabro-review-fable.md`
  and `…-review-sol.md` under the homelab plan directory below.
- Upstream-side claims accepted-as-reported in research/007 MUST be
  re-verified from this repo's primary sources before a filing relies on
  them (007's own instruction).

## Constraints

- **Generic-not-local** (coordination model:
  `homelab/plan/archive/pre-foreman-livespec-hardening/research/002`):
  every capability lands ratified in THIS repo's spec, consumable by any
  adopter, never shaped to homelab paths. homelab is the consumer that
  proves every gate: exit is a consumption demonstration there with a
  negative control; merging is not deploying.
- All filings route through this repo's own propose-change →
  adversarial review → revise lifecycle; implementation children only
  after a scoping event, dispatched factory-first per this repo's
  standing rules.
- Always repo-qualify work-item ids in cross-repo reporting (maintainer
  directive 2026-08-25).

## Tenant state and prior art

This tenant was swept 2026-08-25 twice (homelab-side, and independently
by this session at seeding: 766 records via `bd list --status all
--limit 0 --json`, zero `homelab-loop-hardening` hits) — nothing
pre-exists; there is no earlier epic to find. Prior art to cite in
filings: `bd-ib-ktxb` (v051 attributed valves), `bd-ib-zp3u7y`
(state-machine owner coordination), `bd-ib-tfpdya` +
`bd-ib-5z0g` (acceptance-criteria evaluator), `bd-ib-1dbg` ("Exit
criteria" description fallback), `bd-ib-dohu2g` (executable-as-advertised
lesson).

## Read-first chain

All under `/data/projects/homelab/plan/steady-state-loop-hardening/`:

- `research/002-problem-and-fix-matrix.md` (the ruled-on draft; §§01,
  02, 04, 05, 06, 10, 11, 12, 13, 15 orchestrator rows)
- `research/007-adversarial-review-triage.md` (dispositions; supersedes
  §§04/05 premises)
- `research/008-core-review-triage-and-shared-runtime-rule.md`
  (shared-runtime routing rule; §15 correction)
- `research/009-console-review-triage.md` (R1 population split, R2
  consumer shapes, R3 record repairs)
- `research/010-runtime-review-triage.md` (R4 baseline-first; rt-sol-2
  routing test; runtime charge)
- `research/reviews/livespec-orchestrator-beads-fabro-review-fable.md`
  and `…-review-sol.md`

## Reporting obligation

The plan slug, the REPO-QUALIFIED plan-epic id
(`livespec-orchestrator-beads-fabro/<epic-id>`), and any work items filed
at seeding are reported to homelab's `steady-state-loop-hardening`
session, which records them on `homelab/hl-nkuzaz` and the four consume
items (`homelab/hl-allzdn`, `homelab/hl-m6ytv5`, `homelab/hl-dzm37o`,
`homelab/hl-mfz6ig`) per their first acceptance criterion.
