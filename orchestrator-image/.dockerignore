# Keep the build context tiny: only the Dockerfile, entrypoint, the staged
# `fabro` binary, and the staged plugin scripts tree are needed. Everything
# else is excluded.
*
!Dockerfile
!orchestrator-entrypoint.sh
!fabro
!bd-guard
!bd-guard/**
!plugin-scripts
!plugin-scripts/**
